#### OPTIONS

# lexically sort filenames
--sort-files

# pipe the output through a pager by default
--pager=less

# highlight the matching text
--color

# print a filename heading above each file's results and print a break between
# results from different files
--group

# search case-insensitive unless capitals are specified
--smart-case

# follow symlinks
--follow

#### TYPES

# consider .rake files as ruby
--type-add=ruby=.rake,.ru

# introduce haml to ack
--type-add=ruby=.haml
--type-set=haml=.haml

# also sass / scss
--type-set=sass=.sass
--type-set=scss=.scss

# and handlebars
--type-set=handlebars=.handlebars

# and coffeescript
--type-set=coffeescript=.coffee

# ignore directories for Rails projects
--ignore-dir=log
--ignore-dir=tmp
--ignore-dir=vendor

# add markdown and textile
--type-set=plaintext=.md,.mdown,.markdown,.mkdn,.textile

# plain text CSV files
--type-set=csv=.txt,.csv,.tsv
